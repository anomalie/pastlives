<div class="container-fluid">
  <h4> edit this story</h4>
    <div class="form-group">
      <%= form_for @story do |f| %>
      <strong><%= f.radio_button(:category, "story") %></strong>

      <strong>quote <%= f.radio_button(:category, "quote") %></strong>
      </br>
        <%= f.text_area :text, class: "form-control", rows: "10"%>
       </br>   
        <%= f.submit "Edit story" %>

      <% end %>
</div>
</div>